{
  "name": "learning-mocha-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Tiny DeepSeek API gateway for the Learning Mocha Android app. Holds the API key; stores nothing.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  }
}
