# Copy to .env and fill in. NEVER commit .env — the key must stay server-side.
DEEPSEEK_API_KEY=sk-your-key-here
MODEL=deepseek-chat
PORT=8787
# DEEPSEEK_URL=https://api.deepseek.com/chat/completions
