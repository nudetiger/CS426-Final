<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark mode inherits the whole base theme; the palette already flips through
         values-night/colors.xml, so only the status-bar icon polarity differs. -->
    <style name="Theme.LearningMocha" parent="Theme.Mocha.Base">
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
