<?xml version="1.0" encoding="utf-8"?>
<!-- Adaptive-icon background layer. Literal hex, not @color/mocha_cream: the launcher resolves
     icon drawables against the system configuration, so a DayNight token would flip the icon
     dark at night while every other app icon kept its colour. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#FFF7F0"
        android:pathData="M0,0h108v108h-108z" />
</vector>
